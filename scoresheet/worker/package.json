{
  "name": "aahl-scoresheet-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Auth proxy + API that bridges the AAHL live scoresheet PWA to HomeTeamsOnline",
  "scripts": {
    "test": "node --test test/index.test.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
